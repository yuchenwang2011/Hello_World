All methods in java are virtual by default. 
That means that any method can be overridden when used in inheritance, 
unless that method is declared as final or static.
The equivalent to a pure virtual method is an abstract method.
Nonstatic Java methods are always virtual, but not static methods.
